name: base_custom
channels:
  - conda-forge
dependencies:
  - jupyter
  - jupyterlab
# nb_conda_kernels fucks this up on discover (test on other systems), install manually using
# conda remove zeromq-y; conda install zeromq -y;conda install nb_conda_kernels -y

#  - zeromq #libsodium did not work alone
#- libsodium #experimental fix (for problems with nb_conda_kernels)
#  - nb_conda_kernels
  # every computing env should have ipykernel installed
  - black
  - line_profiler
  - cookiecutter
